@model Houdini.Models.StockRequest

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Stock Request</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StockRequestID)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StockRequestID)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Product.Name)
        </dt>
        <dd>
            @ViewData["itemName"]
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Quantity)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Quantity)
        </dd>

    </dl>

    <form asp-action="DeleteStockRequest">
        <input type="hidden" asp-for="StockRequestID" />
        <input type="submit" value="Delete" class="btn btn-default" />
        <a asp-action="StockRequest">Back to List</a>
    </form>
</div>
